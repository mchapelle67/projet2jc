{{ form_start(searchForm, {'attr': {'class': 'form-inline'}}) }}
    {{ form_widget(searchForm.q, {'attr': {'class': 'form-control mr-sm-2', 'placeholder': 'Rechercher par nom, prenom ou email...', 'aria-label': 'Rechercher'}}) }}
    <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Rechercher</button>
{{ form_end(searchForm) }}